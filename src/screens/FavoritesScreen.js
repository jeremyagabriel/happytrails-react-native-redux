import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const FavoritesScreen = props => {
  return(
    <View>
      <Text>Your Favorite Trails</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  
})

export default FavoritesScreen